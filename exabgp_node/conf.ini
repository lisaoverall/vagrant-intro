process announce-routes {
    run python3 /path/to/pythonscript
    encoder json;
}

neighbor X.X.X.X {                 # Remote neighbor to peer with
    router-id Y.Y.Y.Y;		   # our local router id
    local-address Y.Y.Y.Y;         # Our local update-source
    local-as 12345;                # Our local AS
    peer-as 12345;                 # Peer's AS

  api {
        processes [???];
    }
}
